{
	"name": "infyfix-hack",
	"version": "1.0.0",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"start": "node index.js",
		"dev": "nodemon index.js",
		"build": "npm run prebuild && npm run validate && npm run postbuild",
		"prebuild": "echo '🚀 Starting build process...' && npm ci --only=production",
		"validate": "echo '🔍 Validating application...' && node -c index.js",
		"postbuild": "echo '✅ Build completed successfully! Ready for production deployment.'",
		"build:dev": "npm install && npm run validate"
	},
	"type": "module",
	"keywords": [],
	"author": "",
	"license": "ISC",
	"description": "",
	"dependencies": {
		"@types/express": "^4.17.21",
		"dotenv": "^16.4.5",
		"ejs": "^3.1.10",
		"express": "^4.19.2",
		"mongodb": "^6.8.1",
		"mongoose": "^8.6.1",
		"nodemailer": "^6.9.15",
		"nodemon": "^3.1.4"
	}
}
